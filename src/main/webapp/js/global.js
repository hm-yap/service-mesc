// Path store to be used by modules || Retrieved by using path[key]

define([], function(){

	var global = {

		//Data retrieving path/URL
		navigationBar : 'navigationBar.json',
		tableColumn : 'tableColumn.json',
		tableContent : 'tableContent.json'
	};

	return global;
});